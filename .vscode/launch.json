{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Benteng",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/benteng",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        }
    ]
}
